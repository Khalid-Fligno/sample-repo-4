{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug iOS",
            "request": "attach",
            "type": "reactnative",
            "cwd": "${workspaceFolder}",
        },
        {
            "name": "Attach to packager",
            "request": "attach",
            "type": "reactnative",
            "cwd": "${workspaceFolder}"
        }
    ]
}